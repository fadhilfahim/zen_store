# Required for Supabase (database and storage)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Admin auth (required to access /admin)
ADMIN_PASSWORD=password
# Optional: separate secret used to sign admin session cookie
ADMIN_SESSION_SECRET=password1

# Notifications (optional)
ADMIN_EMAIL=mfadhilfahim@gmail.com
WHATSAPP_WEBHOOK_URL=

# SMTP (optional - if omitted, emails are logged to server console)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
MAIL_FROM=

